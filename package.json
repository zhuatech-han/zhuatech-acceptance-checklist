{
  "name": "zhuatech-acceptance-checklist",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run build && node scripts/dev.mjs",
    "build": "node scripts/build.mjs",
    "lint": "node --check src/core.js && node --check src/app.js && node --check scripts/build.mjs && node --check scripts/dev.mjs && node --check tests/core.test.mjs",
    "test": "node --test tests/*.test.mjs"
  },
  "engines": {
    "node": ">=24"
  }
}
